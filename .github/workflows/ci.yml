name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  prime-canonical:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Setup Foundry
        uses: foundry-rs/foundry-toolchain@v1
        with:
          version: stable

      - name: Install dependencies
        run: npm ci

      - name: Install hardhat dependencies
        run: |
          cd hardhat
          npm ci

      - name: Prime canonical compile/test/size/smoke
        run: npm run test:prime:ci

      - name: UI smoke test
        run: npm run test:ui

  becoming-genesis-556-snapshot:
    if: github.head_ref == 'chatgpt/becoming-genesis-556-snapshot-20260816'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout isolated snapshot branch
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Capture every public OpenSea item URL
        env:
          SNAPSHOT_OUT: snapshot_output
        run: node scripts/opensea-manifest-scrape.mjs

      - name: Upload manifest evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: montrealai-becoming-genesis-556-opensea-manifest
          path: snapshot_output
          if-no-files-found: error
          compression-level: 9
          retention-days: 30
